import Immutable from 'seamless-immutable'

const initialState = Immutable({
  trainlist: {}
})

export default function reduce(state = initialState, action) {
  return (state)
}

export function getDaysList(state) {
  
}
